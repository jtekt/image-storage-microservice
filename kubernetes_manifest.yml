apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: storage
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: storage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: storage
  template:
    metadata:
      labels:
        app: storage
    spec:
      volumes:
      - name: storage
        persistentVolumeClaim:
          claimName: storage
      containers:
      - name: storage
        image: 172.16.98.151:5000/storage
        imagePullPolicy: Always
        env:
        - name: MONGODB_URL
          value: "mongodb://172.16.98.151:27017"
        - name: MONGODB_DB
          value: seikaibu_edge_ai
        ports:
        - containerPort: 80
        volumeMounts:
        - mountPath: "/usr/share/pv"
          name: storage
---
apiVersion: v1
kind: Service
metadata:
  name: storage
spec:
  ports:
  - port: 80
    nodePort: 31221
  selector:
    app: storage
  type: NodePort
